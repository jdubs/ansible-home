# Nuk System — Grafana stack variables

# ── General ─────────────────────────────────────────────────
data_dir: /data0

# ── Certbot AWS Credentials (Route53 DNS validation) ──────────
certbot_aws_access_key_id: "AKIA3S...6PZ2"
certbot_aws_secret_access_key: "IY5SDvEIha/Wdeft7JBcCjiXPRHAO2yq5ba0vLGu"

# ── Docker Images ─────────────────────────────────────────────
grafana_image: grafana/grafana:latest
prometheus_image: prom/prometheus:latest
loki_image: grafana/loki:latest
fluent_bit_image: fluent/fluent-bit:latest
uptime_kuma_image: louislam/uptime-kuma:latest
node_exporter_image: prom/node-exporter:latest

# ── Grafana Admin ─────────────────────────────────────────────
grafana_admin_user: admin
grafana_admin_password: admin
grafana_install_plugins: ""

# ── Prometheus Retention ──────────────────────────────────────
prom_retention: 15d

# ── GitHub OAuth for Grafana ──────────────────────────────────
github_client_id: Ov23li7eQfxhRkAESNNz
github_client_secret: <GH_SECRET>

# ── Grafana Git Sync ──────────────────────────────────────────
# Pulls provisioning files directly from GitHub
grafana_git_sync_repo: "https://github.com/jdubs/ansible-home.git"
grafana_git_sync_branch: main
grafana_git_sync_interval: "60s"
grafana_git_sync_token: <GITHUB_PAT>
grafana_git_sync_repo_path: "nuk/grafana/provisioning"
